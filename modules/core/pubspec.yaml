name: core
description: A new Flutter package project.
version: 0.0.1
homepage:

publish_to: 'none'

environment:
  sdk: '>=2.17.6 <3.0.0'
  flutter: '>=1.17.0'

dependencies:
  flutter:
    sdk: flutter

  model:
    path: ../model

  path_provider: ^2.0.11
  dio: ^4.0.6
  pretty_dio_logger: ^1.1.1
  hive: ^2.2.3
  hive_flutter: ^1.1.0

  # https://firebase.google.com/docs/auth/flutter/federated-auth#ios+-and-android_3
  firebase_auth: ^3.4.2
  google_sign_in: ^5.4.0
  flutter_facebook_auth: ^4.4.0+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

  hive_generator: ^1.1.3
  build_runner: ^2.2.0

flutter:
